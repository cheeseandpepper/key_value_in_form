This is an exercise in adding a generic key/value field to a model. I'm using jsonb as the data type and in the form rendering partials for each k/v pair.

To run the tests type +bundle exec rake test:all+
To view the app on heroku go to: 
